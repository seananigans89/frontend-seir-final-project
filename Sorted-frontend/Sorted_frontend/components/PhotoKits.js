import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const PhotoKits = ({navigation}) => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default PhotoKits

const styles = StyleSheet.create({})
