import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const VideoKits = ({navigation}) => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default VideoKits

const styles = StyleSheet.create({})
