import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Storage = ({nagivation}) => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default Storage

const styles = StyleSheet.create({})
